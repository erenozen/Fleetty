{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Fleetty Theme Extension",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "npm: build",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
