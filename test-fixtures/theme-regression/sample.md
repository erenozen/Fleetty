# Fleetty Theme Fixture

This file checks **bold**, _italic_, `inline code`, and [links](https://example.com).

```python
if source_mode is None:
    source_mode = "unknown"
```

- Editor background should match Fleet's editor surface.
- Python control-flow keywords should use the keyword color.
